apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: mlops-project
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: mlops-project
  filterTags:
    pattern: "^v(.+)$"
    extract: "$1"
  policy:
    semver:
      range: x.x.x